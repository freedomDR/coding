#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n, c; cin >> n >> c;
    vector<int> arr(n), ans;
    for(auto &v:arr) cin >> v;

    for(auto &v:ans) cout << v << " ";
    cout << endl;
    return 0;
}
