#include<iostream>
#include<vector>

using namespace std;

const int mod = 1e9+7;

int main()
{
    int v, k; cin >> v >> k;
    int every_one = 2;
    for(int i = 2; i <= v; i++)
    {
        if(k%i==0) every_one = i;
    }
    string ans = "";
    for(int i = 0; i < k/every_one; i++)
    {
        str
    }
    return 0;
}