#include <bits/stdc++.h>

using namespace std;

vector<int> slove(vector<int>& nums, int k)
{
    auto cmp = []
}
int main()
{
    int n, k;
    cin>>n;
    vector<int> nums(n);
    for(int i = 0; i < n; i++) cin>>num[i];
    cin>>k;
    vector<int> res = slove(nums, k);
    for(int v:res) cout<<v<<" ";
    return 0;
}
